import 'package:flutter/cupertino.dart';

class FreezeUser extends StatelessWidget {
  const FreezeUser({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
